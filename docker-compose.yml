version: "3.8"
services:
  frontend:
    build:
      context: ./frontend/react-app
    ports:
      - "3000:3000"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    volumes:
      - "./backend/database/database.db" # SQLiteデータベースの永続化
